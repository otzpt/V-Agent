# V-Agent — Python dependencies
# Install: pip install -r requirements.txt

# GUI framework (cross-platform, LGPL — free for commercial use)
PySide6>=6.6.0

# HTTP requests (backend communication)
requests>=2.31.0

# Environment variables loader
python-dotenv>=1.0.0
